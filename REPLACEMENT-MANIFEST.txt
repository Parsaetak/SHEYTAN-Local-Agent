SHEYTAN-Local-Agent v1.1.8 — Replacement Manifest
=================================================
Base: main @ dbe3bd9 (v1.1.7)
Package: SHEYTAN-Local-Agent-v1.1.8-UPDATE.zip
Apply over a clean checkout of the base commit.

Status vocabulary: new | modified | deleted | unchanged
The exact per-file change map (with reasons), the DO NOT TOUCH list,
the root cause of Actions run 34788709977, the executed automated
checks and the known limitations live in UPDATE.md.

[PACKAGE METADATA]
  UPDATE.md
    status:   modified
    purpose:  This replacement package's exact change map
  REPLACEMENT-MANIFEST.txt
    status:   modified
    purpose:  This manifest
  REPLACEMENT-SHA256.txt
    status:   modified
    purpose:  SHA-256 of every file in this package

[NEW FILES]
  src/ModelPicker.tsx — redesigned model-selection panel (measured facts, honest classification, real actions only)
  src/PerfStrip.tsx — compact /api/perf telemetry strip for the Agent surface (N/A contract preserved)
  web/static/assets/* (new hashed Vite bundles)

[MODIFIED FILES]
  .github/workflows/build-desktop.yml — CI release-identity fix: runtime derivation from package.json (audit job outputs), no hardcoded APP_VERSION/APP_CODENAME, no -zeta assumption
  scripts/release-version.mjs — --env identity mode + workflow shape check
  package.json — version 1.1.8 (the only manual version edit)
  package-lock.json — root + packages[""] version aligned to 1.1.8
  internal/config/config.go — AppVersion 1.1.8 (script-repaired)
  build/config.yml — productVersion 1.1.8 (script-repaired)
  SIGNATURE — SHEYTAN-Local-Agent v1.1.8 (script-repaired)
  internal/api/server.go — cached model capability resolution; per-model multimodal/native/chatTemplate facts on /api/models
  internal/api/sessioncontext.go — reuses the cached card+caps read
  src/store.ts — WorkspaceMode (chat|agent) + setMode, persisted
  src/AgentHeader.tsx — ModeSwitch segmented control; mode-aware pills
  src/AgentBody.tsx — Chat/Agent layouts, model rail, picker integration, mode-aware composer, PerfStrip mount
  src/MessageStream.tsx — mode-aware empty state
  src/App.tsx — de-chromed navigation, version fallback 1.1.8
  src/api.ts — Model capability fields
  src/SettingsPanel.tsx — Models and Advanced tabs
  src/styles.css — v1.1.8 design section (mode switch, chat rail, model picker, perf strip) + calmer chrome
  README.md — v1.1.8 section + version header
  agent.md — v1.1.8 next-agent notes
  ARCHITECTURE.md — I.8c v1.1.8 truth table
  web/static/.vite/manifest.json — regenerated
  web/static/index.html — regenerated

[DELETED FILES]
  web/static/assets/ — 8 stale hashed Vite bundles superseded by the
  regenerated embedded build (exact list in UPDATE.md §1)

[UNCHANGED]
  Everything else — including agent orchestration, tool system, Coding
  Lab, context planning/protection, memory/recall, research,
  attachments, MCP, scheduler, native engine, llama.cpp integration,
  security restrictions, config copy-on-write, Windows branding (see
  UPDATE.md §5 DO NOT TOUCH).
